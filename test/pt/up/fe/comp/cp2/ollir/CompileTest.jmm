import imp.Obj;
import io;

class Simple {

    public int add(int a, int b){
        int c;
        boolean flag;
        Simple s;
        Obj obj;
        s = new Simple();
        obj = new Obj();
        c = this.test((new Obj()).f(1+3*2, s.test(1)));
        flag = io.test();
        return c;
    }

    public int test(int a){
        return 1;
    }
}